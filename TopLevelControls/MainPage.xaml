<Page x:Class="Vssl.UnoUINugetInAppBuildIssue.TopLevelControls.MainPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="using:Vssl.UnoUINugetInAppBuildIssue.TopLevelControls"
      xmlns:frameworkControls="using:Vssl.VisualFramework.SampleLibrary"
      Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
  <ScrollViewer IsTabStop="True">
    <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
      <StackPanel
        HorizontalAlignment="Center"
        VerticalAlignment="Center">
        <TextBlock AutomationProperties.AutomationId="HelloTextBlock"
          Text="Hello Uno Platform!"
          HorizontalAlignment="Center" />
      </StackPanel>

            <frameworkControls:ReusableControl Grid.Row="1" VerticalAlignment="Center"/>
    </Grid>
  </ScrollViewer>
</Page>
