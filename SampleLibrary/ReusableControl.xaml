<UserControl
    x:Class="Vssl.VisualFramework.SampleLibrary.ReusableControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Vssl.VisualFramework.SampleLibrary"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">
    <UserControl.Resources>
        <local:BooleanToVisibilityConverter x:Key="visibilityConverter" />
    </UserControl.Resources>

    <Grid>
        <TextBlock Text="{x:Bind VM.HintAndTipsText, Mode=OneWay}"
                   Style="{StaticResource HintAndTipsTextStyle}" TextWrapping="WrapWholeWords" TextAlignment="Center"
                   Visibility="{x:Bind VM.UseTipsAndHints, Mode=OneWay, Converter={StaticResource visibilityConverter}}"/>
    </Grid>
</UserControl>
